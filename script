project = QgsProject.instance()

v_telep = project.mapLayersByName('JNK-települések2')[0]
features_telep = v_telep.getFeatures()

v_parking = project.mapLayersByName('amenity_parking_space_Jász-Nagykun-Szolnok megye')[0]
features_parking = v_parking.getFeatures()

for t in features_telep:
    geom = t.geometry()
    for p in features_parking:
        if geom.contains(p.geometry()):
            print(p['full_id'])
    